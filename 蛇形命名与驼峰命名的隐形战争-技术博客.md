# 蛇形命名与驼峰命名的隐形战争：一个让 Token 变 `undefined` 的跨层契约

> 摘要：密码登录成功 → 首页闪现 3 秒 → 踢回登录页。这类"幽灵 Bug"的根因往往不在业务逻辑本身，而在中间层的隐形数据转换。本文记录一个由 `access_token` vs `accessToken` 引起的排障过程，以及它背后的跨层命名契约问题。

---

## 1. 症状

用户反馈：

- **验证码登录** → 正常进入首页，功能一切正常
- **密码登录** → 提示"登录成功"，跳转首页，3 秒后自动弹回登录页

两个登录方式走的是同一个后端、同一个鉴权体系、同一个前端 Token 存储机制。唯一区别是前端调用方式不同。

## 2. 排查

### 2.1 首先确认 Token 是否真的存入了

打开浏览器 DevTools → Application → Local Storage：

```
halfcart_token: undefined
```

`undefined`？不是 `null`，不是空字符串，是字面量字符串 `"undefined"`。

这说明 `setToken()` 被调用了，但传入的参数是 JavaScript 的 `undefined` 值，被 `localStorage.setItem()` 隐式转换为字符串 `"undefined"`。

### 2.2 追踪 Token 来源

验证码登录用的是 `common.js` 封装的 `request()` 函数：

```js
// 验证码登录 — 正常
request('/api/v1/auth/login-by-code', { ... })
  .then(function(res){
    setToken(res.data.accessToken);  // ✅ 驼峰命名，能取到值
  });
```

密码登录在上一轮重构中为了精确控制错误提示，改成了直接用 `fetch()`：

```js
// 密码登录 — 异常
fetch('http://127.0.0.1:8000/api/v1/auth/login-by-password', { ... })
  .then(function(r) { return r.json(); })
  .then(function(data){
    setToken(data.data.accessToken);  // ❌ 驼峰命名，取到 undefined！
  });
```

问题已经浮出水面了——`data.data.accessToken` 是 `undefined`。

### 2.3 后端到底返回了什么？

后端 `login_by_password` 的返回：

```python
return {
    "access_token": token_pair["access_token"],
    "refresh_token": token_pair["refresh_token"],
    "token_type": token_pair["token_type"],
    "user_id": user.id,
    "nickname": user.nickname,
}
```

Python 的惯例是 `snake_case`。FastAPI 的 `APIResponse.success(data=...)` 把这个字典原样塞进 `data` 字段，返回给前端的 JSON 是：

```json
{
  "code": 0,
  "data": {
    "access_token": "eyJhbGci...",
    "refresh_token": "...",
    "user_id": 6,
    "nickname": "??1603"
  }
}
```

### 2.4 那为什么验证码登录能正常取到值？

因为 `request()` 函数在 `common.js` 里有一个关键的中间处理：

```js
// common.js — convertKeys
window.convertKeys = function (obj) {
  if (Array.isArray(obj)) return obj.map(convertKeys);
  if (obj && typeof obj === 'object') {
    var r = {};
    Object.keys(obj).forEach(function (k) {
      r[toCamel(k)] = convertKeys(obj[k]);  // snake_case → camelCase
    });
    return r;
  }
  return obj;
};

// request() 返回前自动转换
if (data && data.data) data.data = convertKeys(data.data);
return data;
```

`convertKeys` 把 `access_token` → `accessToken`、`user_id` → `userId`。所有通过 `request()` 调用的接口，`data` 层字段自动变成驼峰。

但密码登录的 `fetch()` 绕过了 `request()`，也绕过了这个转换。前端代码仍然用驼峰名去取字段，拿到的自然是 `undefined`。

### 2.5 为什么 3 秒后才弹回登录页？

```js
setToken(undefined);  // localStorage 存入 "undefined"

// 首页 requireAuth()
var token = getToken();  // 返回 "undefined"
if (!token) {            // "undefined" 是 truthy 字符串！
  goPage('login.html');  // 不会立即触发
  return false;
}
```

`"undefined"` 作为字符串是 truthy 的，所以 `requireAuth()` 不会立即拦截。

但首页加载后会发 API 请求获取订单列表：

```js
request('/api/v1/radar/list?...');
// → Authorization: Bearer undefined
// → 后端 JWT 校验失败 → 401
// → common.js catch 401 → clearToken() → goPage('login.html')
```

这就是"3 秒后弹回"的完整链路：首页渲染 → API 请求 → 401 → 清 Token → 跳登录。

## 3. 根因总结

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Python 后端  │────▶│  common.js   │────▶│  前端页面     │
│  snake_case  │     │  convertKeys │     │  camelCase   │
│ access_token │     │  (驼峰转换)   │     │ accessToken  │
└──────────────┘     └──────────────┘     └──────────────┘
                              ▲
                              │
                    密码登录用 fetch() 绕过了这一层
```

这不是一个"谁的命名风格更好"的问题，而是一个 **跨层契约** 问题：当项目约定"前端数据层统一使用驼峰命名"时，所有绕过中间层的直接调用都必须在消费端还原这个契约。

## 4. 修复

将 `fetch()` 直接调用中的驼峰字段名改为蛇形，与后端返回格式对齐：

```js
// ❌ 错误：fetch() 返回 snake_case，却用 camelCase 取值
setToken(data.data.accessToken);

// ✅ 正确：与 fetch() 实际返回格式一致
setToken(data.data.access_token);
```

## 5. 更稳健的做法

更好的修复是让 `fetch()` 调用也走统一的响应处理：

```js
// 方案 A：在 fetch 调用后手动做一次键名转换
var d = convertKeys(data.data);
setToken(d.accessToken);

// 方案 B：仍使用 request()，但改进其错误处理
// 让 request() 支持不自动弹 toast 的模式，由调用方自行处理错误消息
```

**方案 B 更优**——它既保留了统一的驼峰转换，也避免了直接 `fetch()` 带来的维护分散。

## 6. 经验教训

1. **封装层存在一定有它的理由。** 绕过它之前先搞清楚它做了什么——`request()` 不只是发 HTTP 请求，它还做了响应格式标准化、401 自动跳转、key 驼峰转换。省掉这层封装是省了几行代码，但引入了隐性不一致。

2. **"undefined 字符串" 是一个经典陷阱。** `localStorage.setItem("token", undefined)` 存入的是字面量 `"undefined"`，它是 truthy 的，不会触发任何空值检查。排查时如果只看 `getToken()` 是否有值（而非看具体值），会完全错过这个 Bug。

3. **命名风格不一致不会导致编译错误，但会在运行时静默失效。** Python 后端团队习惯 `snake_case`，前端团队习惯 `camelCase`，这两者不会在 CI 中报错，不会在 TypeScript 编译时警告，只会在特定调用路径上返回 `undefined`。统一项目约定（如"前端 data 层统一为驼峰"）并确保所有调用路径都遵守这个约定，是避免此类问题的关键。

---

*排查时间：2026-07-18 · 技术栈：FastAPI (snake_case) + Vue 3 + common.js convertKeys · 问题类型：跨层命名契约不一致*
