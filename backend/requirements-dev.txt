# ===========================================================
# HalfCart Backend 测试依赖 (pip-installable)
# ===========================================================
# 安装: pip install -r requirements-dev.txt
# 运行: pytest -v
# 覆盖: pytest --cov=app/services --cov-report=term
# ===========================================================

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
pytest-cov>=5.0
pytest-env>=1.1
