# 半仓智拼 (HalfCart) 项目工程底座设计

---

## 1. docker-compose.yml 设计说明

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     Nginx (80/443)                          │
│                  反向代理 & 静态资源                           │
└──────────────┬──────────────────────────────┬──────────────┘
               │                              │
       /api/*  │                              │ /*
               ▼                              ▼
┌──────────────────────┐          ┌──────────────────────┐
│  backend (FastAPI)   │          │  frontend (Vue3+Nginx)│
│  Port: 8000          │◄────────►│  Port: 3000           │
└──────┬───────┬───────┘          └──────────────────────┘
       │       │       │
       ▼       ▼       ▼
┌──────┐ ┌─────┐ ┌──────────┐ ┌───────────┐
│MySQL │ │Redis│ │Elastic-  │ │RabbitMQ   │
│8.0   │ │7    │ │Search 8  │ │3-management│
│:3306 │ │:6379│ │:9200     │ │:5672:15672│
└──────┘ └─────┘ └──────────┘ └───────────┘
```

### 1.2 服务详细设计

| 服务 | 镜像 | 版本 | 端口 | 用途（对应PRD需求） |
|------|------|------|------|-------------------|
| MySQL | `mysql:8.0.35` | 8.0 | 3306 | 核心交易数据持久化（用户、订单、资金流水）。选8.0为`SELECT FOR UPDATE`行级锁 + 原子运算`UPDATE t_users SET wallet_balance = wallet_balance + ?`提供底层保障（对应PRD 5.3资金安全） |
| Redis | `redis:7.2-alpine` | 7.2 | 6379 | 分布式锁（加入拼单/核销并发控制）、LBS GEO缓存（雷达大厅1.5km查询）、短信验证码存储、接口限流（对应PRD 4.3/4.5/5.1） |
| ElasticSearch | `elasticsearch:8.11.0` | 8.11 | 9200/9300 | 订单geo_distance地理空间搜索、全文检索（商品名/描述）、多条件筛选（品类/时间/状态）、距离排序（对应PRD 4.3 LBS雷达） |
| RabbitMQ | `rabbitmq:3.12-management` | 3.12 | 5672/15672 | AI任务异步解耦（语音/OCR/纠纷）、订单状态变更通知、定时增量对账任务（对应PRD 4.5/4.7/4.8/4.9） |
| Backend | `python:3.11-slim` 自构建 | — | 8000 | FastAPI异步服务，全量业务API（对应PRD全部功能模块） |
| Frontend | `node:20-alpine` 构建 + `nginx:1.25-alpine` | — | 3000 | Vue3+Vite H5 SPA，Nginx托管静态资源（对应PRD 5.2兼容性要求） |
| Nginx | `nginx:1.25-alpine` | 1.25 | 80/443 | 反向代理入口，HTTPS终结，静态资源CDN回源（对应PRD 5.3全链路HTTPS加密） |

### 1.3 挂载卷设计

| 服务 | 挂载路径 | 用途 |
|------|---------|------|
| MySQL | `./data/mysql:/var/lib/mysql` | 数据持久化 |
| MySQL | `./deploy/mysql/init:/docker-entrypoint-initdb.d` | 建表脚本自动初始化 |
| MySQL | `./deploy/mysql/conf.d:/etc/mysql/conf.d` | 自定义my.cnf（字符集utf8mb4、时区Asia/Shanghai） |
| Redis | `./data/redis:/data` | RDB/AOF持久化 |
| Redis | `./deploy/redis/redis.conf:/usr/local/etc/redis/redis.conf` | 自定义配置（内存上限、淘汰策略`allkeys-lru`、密码） |
| ES | `./data/es:/usr/share/elasticsearch/data` | 索引数据持久化 |
| ES | `./deploy/es/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml` | 集群配置（单节点模式、内存限制） |
| RabbitMQ | `./data/rabbitmq:/var/lib/rabbitmq` | 消息持久化 |
| Backend | `./backend:/app` | 代码热加载（开发模式） |
| Frontend构建 | `./frontend:/app` | 源码挂载构建 |
| Frontend运行 | `./frontend/dist:/usr/share/nginx/html` | 构建产物 |
| Nginx | `./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf` | 路由规则 |

### 1.4 健康检查设计

| 服务 | 检查方式 | 检查命令/端点 | 间隔 | 超时 | 重试 |
|------|---------|-------------|------|------|------|
| MySQL | TCP | `mysqladmin ping -h localhost` | 10s | 5s | 5 |
| Redis | TCP | `redis-cli ping` | 10s | 5s | 5 |
| ES | HTTP | `GET http://localhost:9200/_cluster/health` | 30s | 10s | 10 |
| RabbitMQ | TCP | `rabbitmq-diagnostics check_port_connectivity` | 10s | 5s | 5 |
| Backend | HTTP | `GET http://localhost:8000/health` | 15s | 5s | 3 |
| Frontend | HTTP | `GET http://localhost:3000/` | 15s | 5s | 3 |

> **依赖顺序**：MySQL → Redis → ES → RabbitMQ → Backend → Frontend → Nginx  
> 通过 `depends_on` + `condition: service_healthy` 保证启动顺序

### 1.5 网络配置

- **网络名称**：`halfcart-net`（bridge模式）
- **子网**：`172.20.0.0/16`
- **服务间通信**：通过服务名DNS解析（如 `mysql`、`redis`、`elasticsearch`、`rabbitmq`）
- **对外暴露**：仅Nginx的80/443端口对外，其余服务仅容器内网可达

### 1.6 环境变量（关键配置）

```yaml
# 后端服务环境变量
environment:
  - DATABASE_URL=mysql+aiomysql://user:pass@mysql:3306/halfcart
  - REDIS_URL=redis://:pass@redis:6379/0
  - ES_HOST=http://elasticsearch:9200
  - RABBITMQ_URL=amqp://user:pass@rabbitmq:5672/
  - JWT_SECRET_KEY=${JWT_SECRET_KEY}
  - MOCK_PAYMENT=${MOCK_PAYMENT:-true}        # MVP Mock支付开关（PRD 5.4）
  - DIFY_API_BASE=${DIFY_API_BASE}
  - DIFY_API_KEY=${DIFY_API_KEY}
  - SMS_PROVIDER_KEY=${SMS_PROVIDER_KEY}
```

---

## 2. 数据库核心表结构设计

### 2.1 ER关系总览

```
t_users (用户)
  │
  ├── 1:N ── t_orders (订单) [creator_id]
  ├── 1:N ── t_order_participants (参与人) [user_id]
  ├── 1:N ── t_payment_logs (资金流水) [user_id]
  ├── 1:N ── t_withdraw_applications (提现申请) [user_id]
  └── 1:N ── t_chat_messages (聊天消息) [sender_id]

t_orders (订单)
  ├── 1:N ── t_order_participants [order_id]
  ├── 1:N ── t_payment_logs [order_id]
  ├── 1:N ── t_chat_messages [order_id]
  └── 1:1 ── t_verification_codes [order_id]
```

### 2.2 用户表 `t_users`

> 对应PRD 3.2 虚拟账本资产体系、4.1 用户鉴权

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 用户ID |
| `phone` | VARCHAR(128) | NOT NULL, UNIQUE, INDEX `idx_phone` | 手机号（AES加密存储，PRD 5.3数据安全） |
| `phone_hash` | CHAR(64) | NOT NULL, UNIQUE, INDEX `idx_phone_hash` | 手机号SHA256哈希，用于快速查找（避免解密） |
| `nickname` | VARCHAR(30) | NOT NULL, DEFAULT '' | 用户昵称 |
| `avatar_url` | VARCHAR(255) | DEFAULT '' | 头像URL |
| `credit_score` | TINYINT UNSIGNED | NOT NULL, DEFAULT 100 | 信用分（0-120，PRD 3.2信用分体系） |
| `wallet_balance` | DECIMAL(12,2) | NOT NULL, DEFAULT 0.00, INDEX `idx_wallet_balance` | 可提现钱包余额（明文，支持原子运算，PRD 5.3） |
| `frozen_balance` | DECIMAL(12,2) | NOT NULL, DEFAULT 0.00, INDEX `idx_frozen_balance` | 冻结担保金（明文，PRD 3.2） |
| `status` | TINYINT | NOT NULL, DEFAULT 1 | 1-正常, 0-封禁 |
| `last_login_at` | DATETIME | DEFAULT NULL | 最后登录时间 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引**：
- `uk_phone_hash` UNIQUE —— 手机号去重与快速登录
- `idx_credit_score` —— 信用分排序（PRD 4.3 LBS雷达排序加权）
- `idx_status` —— 账号封禁过滤

**设计说明**：
- `wallet_balance` 和 `frozen_balance` 明文存储，通过数据库访问白名单 + 应用层参数验签 + 全量流水对账三重保障（PRD 5.3产品决策）
- `phone` 加密存储保证隐私，`phone_hash` 哈希索引保证查询性能

### 2.3 订单表 `t_orders`

> 对应PRD 4.2 订单状态机、4.4 双视角订单详情、4.7 AI发单、4.8 现货转让

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | 订单ID |
| `order_no` | CHAR(22) | NOT NULL, UNIQUE, INDEX `uk_order_no` | 业务单号（格式：HC + 时间戳 + 随机串） |
| `creator_id` | BIGINT UNSIGNED | NOT NULL, FK→t_users.id, INDEX `idx_creator_id` | 团长用户ID |
| `status` | TINYINT | NOT NULL, DEFAULT 0, INDEX `idx_status` | 0-GATHERING, 1-PURCHASING, 2-DELIVERING, 3-FINISHED（PRD 4.2四态） |
| `order_type` | TINYINT | NOT NULL, DEFAULT 1 | 1-普通拼单, 2-现货转让（PRD 4.8） |
| `goods_name` | VARCHAR(100) | NOT NULL | 商品名称（必填，PRD 4.7 AI提取） |
| `goods_desc` | VARCHAR(500) | DEFAULT '' | 商品描述 |
| `goods_category` | VARCHAR(30) | DEFAULT '', INDEX `idx_goods_category` | 商品品类（LBS筛选用，PRD 3.1） |
| `purchase_channel` | VARCHAR(50) | DEFAULT '' | 采购渠道（如"山姆会员店"，PRD 4.7可选字段） |
| `total_people` | SMALLINT UNSIGNED | NOT NULL | 需要总人数（含团长，≥2） |
| `current_people` | SMALLINT UNSIGNED | NOT NULL, DEFAULT 1 | 已加入人数 |
| `price_per_person` | DECIMAL(10,2) | NOT NULL | 人均价格（PRD 4.3列表展示） |
| `total_amount` | DECIMAL(12,2) | NOT NULL | 订单总金额 = price_per_person × (total_people - 1) |
| `meet_location` | VARCHAR(200) | NOT NULL | 交接地点（脱敏存储，PRD 4.7必填） |
| `location_lat` | DECIMAL(10,7) | NOT NULL | 交接点纬度（LBS匹配，ES索引） |
| `location_lng` | DECIMAL(10,7) | NOT NULL | 交接点经度（LBS匹配，ES索引） |
| `location_geo` | POINT | — | MySQL空间索引字段（`SRID 4326`），用于辅助LBS查询 |
| `deadline` | DATETIME | NOT NULL, INDEX `idx_deadline` | 截单时间（PRD 4.2状态0自动解散依据） |
| `receipt_img` | VARCHAR(255) | DEFAULT '' | 小票原图URL（现货转让，PRD 4.8） |
| `original_price` | DECIMAL(10,2) | DEFAULT NULL | 小票原价（现货转让，PRD 4.8） |
| `is_spot` | TINYINT | NOT NULL, DEFAULT 0 | 现货标记（0-否, 1-是，PRD 4.8雷达标签） |
| `spot_valid_until` | DATETIME | DEFAULT NULL | 现货有效期（默认24h，PRD 4.8） |
| `dispute_status` | TINYINT | NOT NULL, DEFAULT 0 | 0-无纠纷, 1-处理中, 2-已结案（PRD 4.9） |
| `finished_at` | DATETIME | DEFAULT NULL | 核销完成时间 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE, INDEX `idx_updated_at` | 更新时间（PRD 4.5对账扫描依据） |

**索引**：
- `uk_order_no` UNIQUE —— 业务单号唯一
- `idx_creator_id` —— 团长维度查询（我的发起）
- `idx_status` —— 状态筛选（LBS雷达仅查状态0）
- `idx_status_creator` 复合索引 `(status, creator_id)` —— LBS雷达"非本人+状态0"过滤
- `idx_deadline` —— 定时任务扫描过期订单
- `idx_updated_at` —— **增量对账的核心索引**（PRD 4.5：仅扫近24h活跃订单）
- `idx_goods_category` —— 品类筛选
- `idx_location` SPATIAL —— MySQL空间索引辅助LBS

### 2.4 订单参与人表 `t_order_participants`

> 对应PRD 4.2 订单状态机、4.4 双视角、4.6 阶梯退款

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | — |
| `order_id` | BIGINT UNSIGNED | NOT NULL, FK→t_orders.id | 订单ID |
| `user_id` | BIGINT UNSIGNED | NOT NULL, FK→t_users.id | 拼友用户ID |
| `role` | TINYINT | NOT NULL | 1-团长(creator), 2-拼友(participant) |
| `pay_status` | TINYINT | NOT NULL, DEFAULT 0 | 0-待支付, 1-已支付(资金冻结), 2-已退款, 3-已结算(核销完成) |
| `pay_amount` | DECIMAL(10,2) | NOT NULL | 实付金额 |
| `refund_amount` | DECIMAL(10,2) | DEFAULT NULL | 退款金额 |
| `refund_ratio` | DECIMAL(5,4) | DEFAULT NULL | 退款比例（1.0=全额, 0.7=70%，PRD 4.6） |
| `refund_reason` | VARCHAR(200) | DEFAULT '' | 退款原因 |
| `refunded_at` | DATETIME | DEFAULT NULL | 退款时间 |
| `joined_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 加入时间 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | — |
| `updated_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP ON UPDATE | — |

**索引**：
- `uk_order_user` UNIQUE `(order_id, user_id)` —— 同一订单同一用户仅一条参与记录
- `idx_user_id_status` 复合索引 `(user_id, pay_status)` —— 拼友维度订单查询（"我的参与"，PRD 3.1个人中心）
- `idx_order_id_role` 复合索引 `(order_id, role)` —— 订单参与人列表查询

**业务约束**：
- 单笔订单团长固定1人（role=1），拼友上限 = `total_people - 1`
- 参与人数不超 `total_people` 由 Redis分布式锁 + `SELECT COUNT(*) FOR UPDATE` 双重保证（PRD 5.1并发安全）

### 2.5 资金流水表 `t_payment_logs`

> 对应PRD 3.2 虚拟账本、4.5 资金结算、5.3 资金安全对账

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | — |
| `log_no` | CHAR(26) | NOT NULL, UNIQUE, INDEX `uk_log_no` | 流水单号（全局唯一） |
| `user_id` | BIGINT UNSIGNED | NOT NULL, FK→t_users.id, INDEX `idx_user_id` | 用户ID |
| `order_id` | BIGINT UNSIGNED | DEFAULT NULL, FK→t_orders.id, INDEX `idx_order_id` | 关联订单ID（提现流水不关联订单） |
| `type` | TINYINT | NOT NULL | 1-支付冻结, 2-退款解冻, 3-核销结算, 4-违约金收入, 5-服务费扣除, 6-提现冻结, 7-提现打款, 8-提现退回, 9-纠纷退款 |
| `amount` | DECIMAL(12,2) | NOT NULL | 变动金额（正=入账，负=出账） |
| `balance_before` | DECIMAL(12,2) | NOT NULL | 变动前余额（对应钱包余额或冻结余额） |
| `balance_after` | DECIMAL(12,2) | NOT NULL | 变动后余额 |
| `target_balance_type` | TINYINT | NOT NULL | 变动目标账户：1-wallet_balance, 2-frozen_balance |
| `status` | TINYINT | NOT NULL, DEFAULT 1 | 1-成功, 0-失败（事务回滚） |
| `remark` | VARCHAR(255) | DEFAULT '' | 备注 |
| `idempotent_key` | CHAR(64) | NOT NULL, UNIQUE, INDEX `uk_idempotent_key` | 幂等键（SHA256(业务场景+订单号+用户ID+操作类型)），PRD 5.3幂等设计 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP, INDEX `idx_created_at` | 创建时间 |

**索引**：
- `uk_log_no` UNIQUE —— 流水号唯一
- `uk_idempotent_key` UNIQUE —— **幂等防护核心**，同一操作不重复记账
- `idx_user_id_created` 复合索引 `(user_id, created_at)` —— 用户流水列表分页查询
- `idx_order_id` —— 按订单查流水
- `idx_type_created` 复合索引 `(type, created_at)` —— 按流水类型统计
- `idx_created_at` —— 对账任务扫描

### 2.6 核销码表 `t_verification_codes`

> 对应PRD 4.5 核销码生成与验证

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | — |
| `order_id` | BIGINT UNSIGNED | NOT NULL, UNIQUE, FK→t_orders.id | 订单ID（一对一） |
| `code` | CHAR(6) | NOT NULL, INDEX `idx_code` | 6位数字核销码 |
| `is_used` | TINYINT | NOT NULL, DEFAULT 0 | 0-未使用, 1-已使用（一次性，PRD 4.5） |
| `error_count` | TINYINT UNSIGNED | NOT NULL, DEFAULT 0 | 连续错误次数（≥5锁定10分钟，PRD 4.5异常处理） |
| `locked_until` | DATETIME | DEFAULT NULL | 锁定截止时间 |
| `used_at` | DATETIME | DEFAULT NULL | 核销时间 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | — |

**设计说明**：核销码单独建表而非存订单表内，避免核销操作行锁影响订单主表并发查询。

### 2.7 提现申请表 `t_withdraw_applications`

> 对应PRD 3.2 MVP提现规则、4.10 个人中心提现

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | — |
| `withdraw_no` | CHAR(22) | NOT NULL, UNIQUE | 提现单号 |
| `user_id` | BIGINT UNSIGNED | NOT NULL, FK→t_users.id, INDEX `idx_user_id` | 用户ID |
| `amount` | DECIMAL(10,2) | NOT NULL | 提现金额（≥10元，≤wallet_balance，PRD 4.10） |
| `pay_method` | TINYINT | NOT NULL | 1-微信, 2-支付宝 |
| `pay_account` | VARCHAR(255) | NOT NULL | 收款账号（AES加密存储，PRD 5.3） |
| `real_name` | VARCHAR(100) | NOT NULL | 收款人真实姓名（AES加密存储） |
| `status` | TINYINT | NOT NULL, DEFAULT 0, INDEX `idx_status` | 0-提现中, 1-已完成, 2-已驳回（PRD 4.10） |
| `audit_remark` | VARCHAR(255) | DEFAULT '' | 审核备注 |
| `audited_at` | DATETIME | DEFAULT NULL | 审核时间 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 申请时间 |

**索引**：
- `idx_user_id_status` 复合索引 `(user_id, status)` —— 查询用户处理中申请（同一用户仅允许一笔处理中，PRD 4.10）

### 2.8 聊天消息表 `t_chat_messages`

> 对应PRD 3.1 履约通信、4.4 双视角IM、阅后即焚

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT UNSIGNED | PK, AUTO_INCREMENT | — |
| `order_id` | BIGINT UNSIGNED | NOT NULL, FK→t_orders.id | 订单ID（IM以订单为会话单元） |
| `sender_id` | BIGINT UNSIGNED | NOT NULL, FK→t_users.id | 发送者ID |
| `receiver_id` | BIGINT UNSIGNED | NOT NULL, FK→t_users.id | 接收者ID |
| `message_type` | TINYINT | NOT NULL, DEFAULT 1 | 1-文本, 2-图片, 3-系统通知 |
| `content` | TEXT | NOT NULL | 消息内容（PRD 5.3：订单完成24h后物理销毁） |
| `is_read` | TINYINT | NOT NULL, DEFAULT 0 | 0-未读, 1-已读 |
| `created_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP, INDEX `idx_created_at` | 发送时间 |

**索引**：
- `idx_order_id_created` 复合索引 `(order_id, created_at)` —— 按会话拉取历史消息（WebSocket 1v1聊天，PRD 3.1）
- `idx_created_at` —— **阅后即焚定时任务扫描**（订单FINISHED 24h后物理删除，PRD 3.1）

**设计说明**：
- 不设外键CASCADE删除，由定时任务显式执行 `DELETE` 并记录销毁日志
- 消息不加密存储（非敏感内容），但通过应用层脱敏处理（PRD 5.3隐私保护）

### 2.9 补充表

| 表名 | 用途 | 对应PRD |
|------|------|--------|
| `t_sms_codes` | 短信验证码记录（phone_hash + code + expires_at + send_count），Redis缓存为主，MySQL兜底 | PRD 4.1 |
| `t_user_tokens` | Refresh Token黑名单（token_hash + expires_at），用于JWT双Token机制的登出/过期管理 | PRD 4.1 |
| `t_disputes` | 纠纷记录表（order_id + applicant_id + dispute_type + description + images + ai_judge_result + judge_confidence + final_result），详情在V3.2阶段完善 | PRD 4.9 |
| `t_spot_transfer_parent` | 现货转让父子订单关联（parent_receipt_img + child_order_ids），支撑一张小票拆分5单的约束 | PRD 4.8 |
| `t_user_location_log` | 用户定位日志（user_id + lat + lng + gps_accuracy + ip + is_anomaly），支撑反欺诈规则 | PRD 4.3 反欺诈 |

---

## 3. ElasticSearch 索引设计

### 3.1 索引：`order_index`

> **对应PRD 4.3 LBS雷达大厅**：支持 geo_distance 1.5km范围查询、全文检索、品类/时间多条件筛选、距离排序。  
> **数据来源**：订单创建/状态变更时，通过RabbitMQ异步同步至ES（保证MySQL为主，ES为检索副本）。

### 3.2 字段定义

| 字段名 | ES类型 | 用途 | 对应PRD场景 |
|--------|--------|------|------------|
| `order_id` | `long` | 订单ID（主键） | 关联MySQL |
| `order_no` | `keyword` | 业务单号 | 精确匹配 |
| `creator_id` | `long` | 团长用户ID | **越权拦截**：过滤 `creator_id != 当前用户ID`（PRD 3.1） |
| `status` | `integer` | 订单状态 | **过滤**：仅查status=0（GATHERING，PRD 4.3） |
| `order_type` | `integer` | 订单类型 | **过滤**：1-普通拼单, 2-现货转让 |
| `goods_name` | `text` + `keyword` | 商品名称 | **全文检索**（`match`查询） + 精确匹配（`term`查询） |
| `goods_desc` | `text` | 商品描述 | **全文检索**补充字段 |
| `goods_category` | `keyword` | 商品品类 | **多条件筛选**（PRD 3.1 拼单筛选） |
| `price_per_person` | `scaled_float` (2位小数) | 人均价格 | 筛选范围、排序 |
| `total_people` | `integer` | 总人数 | 列表展示（如"还差3人"） |
| `current_people` | `integer` | 当前人数 | 计算剩余名额 |
| `location` | `geo_point` | 交接坐标(lat, lon) | **geo_distance 地理查询**（PRD 4.3 1.5km范围） |
| `location_text` | `text` | 交接地点文本（脱敏） | 列表展示 |
| `deadline` | `date` | 截单时间 | **过滤**：未过期订单 |
| `credit_score` | `integer` | 团长信用分 | **排序加权**：信用分高者前置（PRD 4.3排序规则） |
| `is_spot` | `boolean` | 是否现货 | **过滤 + 加权排序**：现货订单前置（PRD 4.8） |
| `spot_valid_until` | `date` | 现货有效期 | 过滤：现货未过期 |
| `created_at` | `date` | 创建时间 | 排序、时效性加权 |
| `distance` | — (script字段) | **运行时计算**：当前用户位置到订单位置的距离 | **核心排序字段**：按距离升序（PRD 4.3） |

### 3.3 核心查询DSL设计

**LBS雷达大厅查询逻辑**（对应PRD 4.3主流程）：

```json
{
  "query": {
    "bool": {
      "must": [
        { "term": { "status": 0 } },
        { "range": { "deadline": { "gte": "now" } } }
      ],
      "must_not": [
        { "term": { "creator_id": "<当前用户ID>" } }
      ],
      "filter": {
        "geo_distance": {
          "distance": "1.5km",
          "location": { "lat": "<用户纬度>", "lon": "<用户经度>" }
        }
      },
      "should": [
        { "term": { "is_spot": true } },
        { "match": { "goods_name": "<搜索关键词>" } },
        { "term": { "goods_category": "<品类>" } }
      ]
    }
  },
  "sort": [
    {
      "_geo_distance": {
        "location": { "lat": "<用户纬度>", "lon": "<用户经度>" },
        "order": "asc",
        "unit": "m",
        "mode": "min"
      }
    },
    { "credit_score": { "order": "desc" } },
    { "is_spot": { "order": "desc" } }
  ]
}
```

### 3.4 索引设置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `number_of_shards` | 3 | 分片数（初期数据量预估10w级订单，3分片够用） |
| `number_of_replicas` | 1 | 副本数（高可用） |
| `refresh_interval` | `5s` | 近实时刷新（LBS对实时性要求较高） |
| `max_result_window` | 10000 | 分页上限 |

### 3.5 ES与MySQL数据同步策略

```
订单创建/状态变更
       │
       ▼
  MySQL 写入成功
       │
       ▼
  RabbitMQ 发送同步消息 ──→ Consumer 消费 ──→ ES upsert
       │                                         │
       ▼                                         ▼
  事务提交                                  索引即时更新
```

- **同步方式**：异步准实时（RabbitMQ解耦），延迟 < 1s
- **容错**：Consumer失败重试3次 + 死信队列兜底 + 定时全量对齐（每天凌晨3点）
- **一致性**：ES为检索副本，以MySQL为准；ES数据丢失不影响核心交易（仅影响LBS搜索体验）

---

## 4. RabbitMQ 队列规划

### 4.1 队列全景图

```
                     ┌─→ ai.asr.order ───────────→ asr_consumer
                     │    (语音识别+NLP解析)           Dify ASR → 结构化 → 创建订单
                     │
                     ├─→ ai.ocr.spot ─────────────→ ocr_consumer
                     │    (小票OCR识别)                Dify OCR → 结构化 → 创建现货订单
                     │
                     ├─→ ai.dispute.judge ─────────→ dispute_consumer
                     │    (纠纷多模态裁判)              Dify Vision → 判责 → 自动结算
                     │
publisher ──→ halfcart.exchange (topic) ──┤
(后端服务)                                   ├─→ order.notify ────────────→ notify_consumer
                     │    (订单状态变更通知)            WebSocket推送 / 短信通知
                     │
                     ├─→ order.status.sync ────────→ es_sync_consumer
                     │    (ES索引同步)                 ES upsert订单文档
                     │
                     ├─→ order.reconcile ───────────→ reconcile_consumer
                     │    (增量对账任务)                资金流水 vs 订单状态 vs 用户余额
                     │
                     └─→ order.chat.cleanup ────────→ cleanup_consumer
                          (阅后即焚清理)                24h后DELETE聊天记录
```

### 4.2 队列详细设计

| 队列名称 | 交换机 | Routing Key | 类型 | 业务场景（对应PRD） | 并发消费者 | 消费逻辑 |
|----------|--------|-------------|------|--------------------|-----------|---------|
| `ai.asr.order` | `halfcart.exchange` | `ai.asr.order` | topic | **AI语音/文本发单**（PRD 4.7） | 3 | 1. 消费音频文件/文本 2. 调用ASR转写 3. 透传Dify NLP工作流 4. 解析结构化JSON 5. 校验字段完整性 6. 创建GATHERING订单 7. 推送结果通知 |
| `ai.ocr.spot` | `halfcart.exchange` | `ai.ocr.spot` | topic | **OCR现货转让**（PRD 4.8） | 2 | 1. 消费小票图片 2. 透传Dify多模态OCR 3. 提取商品名/数量/价格 4. 校验转让价≤原价 5. 创建DELIVERING现货订单 6. 推送结果通知 |
| `ai.dispute.judge` | `halfcart.exchange` | `ai.dispute.judge` | topic | **AI纠纷裁判**（PRD 4.9） | 2 | 1. 消费纠纷描述+图片 2. 透传Dify多模态裁判 3. 获取判责比例 4. 置信度≥80%自动执行资金划转 5. <80%或≥500元转人工队列 6. 推送判责结果 |
| `order.notify` | `halfcart.exchange` | `order.notify` | topic | **订单状态变更通知**（PRD 4.2/4.4/4.5） | 2 | 1. 消费状态变更事件 2. WebSocket实时推送双方 3. 短信通知关键节点（核销码生成、核销成功） 4. 应用内消息中心记录 |
| `order.status.sync` | `halfcart.exchange` | `order.status.sync` | topic | **ES索引同步**（PRD 4.3 LBS雷达） | 2 | 1. 消费订单创建/状态变更事件 2. ES upsert订单文档 3. 失败重试3次 → 死信队列 |
| `order.reconcile` | `halfcart.exchange` | `order.reconcile` | topic | **增量对账**（PRD 4.5分布式事务补偿） | 1 | 1. 每小时触发一次 2. 扫描近24h内`updated_at`变更的活跃订单 3. 比对支付流水、订单状态、用户余额三者一致性 4. 不一致则自动补偿修正 5. 无法补偿的触发告警 |
| `order.chat.cleanup` | `halfcart.exchange` | `order.chat.cleanup` | topic | **阅后即焚清理**（PRD 3.1 履约通信） | 1 | 1. 定时扫描FINISHED超24h订单 2. 物理DELETE聊天记录 3. 记录销毁日志 |

### 4.3 死信队列 (DLQ) 设计

| DLQ名称 | 对应业务队列 | 死信策略 |
|---------|------------|---------|
| `dlq.ai.asr` | `ai.asr.order` | 重试3次后进入DLQ，人工检查ASR/NLP失败原因，手动重发或降级 |
| `dlq.ai.ocr` | `ai.ocr.spot` | 重试3次后进入DLQ，通知用户降级为手动填写 |
| `dlq.ai.dispute` | `ai.dispute.judge` | 重试3次后进入DLQ，自动转人工审核队列 |
| `dlq.order.sync` | `order.status.sync` | 重试3次后进入DLQ，次日凌晨全量对齐兜底 |
| `dlq.order.reconcile` | `order.reconcile` | 重试1次后进入DLQ，触发运营告警 |

### 4.4 消息格式规范

所有消息体统一JSON格式：
```json
{
  "event_id": "uuid",
  "event_type": "order.created | order.status_changed | payment.frozen | ...",
  "timestamp": "2026-07-02T10:30:00Z",
  "source": "halfcart-backend",
  "payload": { ... },
  "trace_id": "uuid"
}
```

- `event_id`：消息唯一ID，消费端幂等去重
- `trace_id`：全链路追踪ID，关联前端→后端→MQ→AI的完整调用链

---

## 5. FastAPI 项目目录结构

### 5.1 目录树

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                          # FastAPI 应用工厂、生命周期管理、全局中间件注册
│   ├── config.py                        # 配置中心（Pydantic Settings，各环境配置读取）
│   │
│   ├── api/                             # ── 接口层（Router） ──
│   │   ├── __init__.py
│   │   ├── deps.py                      # 依赖注入（get_db, get_current_user, get_redis, get_es, get_mq）
│   │   ├── v1/                          # API v1 版本
│   │   │   ├── __init__.py
│   │   │   ├── router.py               # v1 路由聚合
│   │   │   ├── auth.py                 # 鉴权接口（登录/登出/刷新Token）── PRD 4.1
│   │   │   ├── users.py                # 用户接口（个人信息/信用分查询）── PRD 3.1/3.2
│   │   │   ├── orders.py               # 订单接口（创建/查询/状态流转）── PRD 4.2/4.7/4.8
│   │   │   ├── participants.py         # 参与人接口（加入/支付/退出/退款）── PRD 4.4/4.6
│   │   │   ├── radar.py                # LBS雷达接口（附近拼单列表/筛选）── PRD 4.3
│   │   │   ├── verification.py         # 核销接口（生成/验证核销码）── PRD 4.5
│   │   │   ├── payment.py              # 支付接口（Mock支付/真实渠道预留）── PRD 4.5
│   │   │   ├── withdraw.py             # 提现接口（申请/查询）── PRD 4.10
│   │   │   ├── disputes.py             # 纠纷接口（发起/查询判责结果）── PRD 4.9
│   │   │   ├── chat.py                 # 聊天接口（WebSocket连接/历史消息）── PRD 3.1
│   │   │   └── admin.py                # 运营后台接口（订单查询/提现审核/信用分管控）── PRD 3.1
│   │   └── websocket/                  # WebSocket 路由
│   │       ├── __init__.py
│   │       └── chat_ws.py              # 1v1聊天WebSocket handler ── PRD 3.1
│   │
│   ├── core/                            # ── 核心基础设施层 ──
│   │   ├── __init__.py
│   │   ├── security.py                 # JWT Token生成/验证、密码哈希、手机号加密/哈希
│   │   ├── database.py                 # SQLAlchemy 2.0 async engine + session工厂
│   │   ├── redis_client.py             # Redis 连接池（aioredis）、分布式锁封装、GEO命令封装
│   │   ├── es_client.py                # ElasticSearch 8 async client、索引管理
│   │   ├── mq_client.py                # RabbitMQ (aio-pika) 连接管理、发布封装
│   │   ├── dify_client.py              # Dify API 异步客户端（NLP/OCR/裁判三个工作流调用）
│   │   ├── sms_client.py               # 短信服务商异步客户端
│   │   └── limiter.py                  # 接口限流（基于Redis的滑动窗口）── PRD 4.1/5.1
│   │
│   ├── models/                          # ── 数据模型层（SQLAlchemy ORM） ──
│   │   ├── __init__.py
│   │   ├── base.py                     # Base模型（id, created_at, updated_at）
│   │   ├── user.py                     # t_users ── PRD 3.2
│   │   ├── order.py                    # t_orders ── PRD 4.2
│   │   ├── order_participant.py        # t_order_participants ── PRD 4.2/4.4
│   │   ├── payment_log.py             # t_payment_logs ── PRD 3.2/4.5
│   │   ├── verification_code.py       # t_verification_codes ── PRD 4.5
│   │   ├── withdraw.py                # t_withdraw_applications ── PRD 4.10
│   │   ├── chat_message.py            # t_chat_messages ── PRD 3.1
│   │   ├── sms_code.py                # t_sms_codes ── PRD 4.1
│   │   ├── user_token.py              # t_user_tokens ── PRD 4.1
│   │   ├── dispute.py                 # t_disputes ── PRD 4.9
│   │   └── user_location_log.py       # t_user_location_log ── PRD 4.3
│   │
│   ├── schemas/                         # ── Pydantic Schema层（请求/响应校验） ──
│   │   ├── __init__.py
│   │   ├── auth.py                     # 登录/注册/Token请求响应
│   │   ├── user.py                     # 用户信息/信用分
│   │   ├── order.py                    # 订单创建(AI解析后确认)/查询/详情
│   │   ├── participant.py              # 加入/支付/退款
│   │   ├── radar.py                    # LBS查询参数/列表卡片响应
│   │   ├── verification.py            # 核销码验证
│   │   ├── payment.py                  # 支付请求/回调
│   │   ├── withdraw.py                 # 提现申请/查询
│   │   ├── dispute.py                  # 纠纷发起/判责结果
│   │   ├── chat.py                     # 消息发送/历史查询
│   │   └── common.py                   # 通用分页/响应包装
│   │
│   ├── services/                        # ── 业务逻辑层 ──
│   │   ├── __init__.py
│   │   ├── auth_service.py             # 登录/注册/Token刷新逻辑
│   │   ├── user_service.py             # 用户信息/信用分变更
│   │   ├── order_service.py            # 订单创建(AI解析)/状态机流转/解散
│   │   ├── participant_service.py      # 加入拼单/支付/退款（分布式锁+行锁）
│   │   ├── radar_service.py            # LBS查询（ES geo_distance + Redis GEO缓存）
│   │   ├── verification_service.py     # 核销码生成/验证/锁定
│   │   ├── settlement_service.py       # 资金结算（扣服务费/余额划转/幂等）
│   │   ├── refund_service.py           # 阶梯退款（状态判断+比例计算+违约金划转）
│   │   ├── withdraw_service.py         # 提现申请/余额校验
│   │   ├── dispute_service.py          # 纠纷发起/AI裁判结果处理
│   │   ├── chat_service.py             # 消息存储/查询/阅后即焚
│   │   ├── notification_service.py     # 消息推送（WebSocket + 短信）
│   │   └── reconcile_service.py        # 增量对账（流水 vs 订单 vs 余额一致性校验）
│   │
│   ├── tasks/                           # ── 后台任务（MQ消费者 + 定时任务） ──
│   │   ├── __init__.py
│   │   ├── consumer_base.py            # MQ消费者基类（连接管理、重连、优雅关闭）
│   │   ├── asr_consumer.py             # AI语音发单消费者 ── PRD 4.7
│   │   ├── ocr_consumer.py             # OCR现货转让消费者 ── PRD 4.8
│   │   ├── dispute_consumer.py         # AI纠纷裁判消费者 ── PRD 4.9
│   │   ├── notify_consumer.py          # 订单通知消费者 ── PRD 4.2
│   │   ├── es_sync_consumer.py         # ES同步消费者 ── PRD 4.3
│   │   ├── reconcile_consumer.py       # 增量对账消费者 ── PRD 4.5
│   │   ├── chat_cleanup_consumer.py    # 阅后即焚清理消费者 ── PRD 3.1
│   │   └── scheduler.py                # 定时任务调度（APScheduler：超时订单解散、现货过期下架）
│   │
│   ├── middleware/                      # ── 中间件 ──
│   │   ├── __init__.py
│   │   ├── cors.py                     # CORS配置（移动端域名白名单）
│   │   ├── trace.py                    # 请求trace_id注入 + 全链路追踪日志
│   │   ├── exception_handler.py        # 全局异常处理（业务异常/系统异常 → 统一响应格式）
│   │   └── request_log.py              # 请求日志记录（核心接口全量、非核心接口采样）
│   │
│   ├── utils/                           # ── 工具函数 ──
│   │   ├── __init__.py
│   │   ├── geo_utils.py                # 坐标计算（Haversine距离、坐标合法性校验）── PRD 4.3
│   │   ├── idempotent.py               # 幂等键生成与校验 ── PRD 5.3
│   │   ├── privacy.py                  # 手机号脱敏(13x****xxxx)、地址脱敏 ── PRD 5.3
│   │   ├── code_generator.py           # 6位核销码/验证码生成
│   │   └── constants.py                # 枚举常量（订单状态、流水类型、纠纷类型等）
│   │
│   └── migrations/                      # ── 数据库迁移（Alembic） ──
│       ├── env.py
│       ├── alembic.ini
│       └── versions/                    # 迁移脚本版本文件
│
├── tests/                               # ── 测试目录 ──
│   ├── conftest.py                     # pytest fixtures（test_db, test_redis, test_client）
│   ├── test_auth.py                    # 鉴权模块测试
│   ├── test_orders.py                  # 订单状态机测试
│   ├── test_participants.py            # 加入拼单并发测试（100并发，PRD 8.1验收标准7）
│   ├── test_settlement.py              # 资金结算精度测试
│   ├── test_refund.py                  # 阶梯退款测试
│   ├── test_radar.py                   # LBS雷达测试
│   ├── test_verification.py            # 核销码安全测试
│   └── test_reconcile.py               # 对账补偿测试（PRD 8.1验收标准11）
│
├── Dockerfile                          # 后端容器构建
├── pyproject.toml                      # Python项目配置（依赖管理：Poetry）
├── .env.example                        # 环境变量模板
└── README.md
```

### 5.2 分层架构与职责说明

```
┌─────────────────────────────────────────┐
│          api/ (接口层 / Router)           │
│  职责：HTTP请求解析、参数校验、鉴权、       │
│        调用Service、组装响应              │
│  约束：不写任何业务逻辑                    │
└────────────────┬────────────────────────┘
                 │ 调用
                 ▼
┌─────────────────────────────────────────┐
│        services/ (业务逻辑层)             │
│  职责：核心业务编排、状态机流转、           │
│        分布式锁获取释放、事务管理          │
│  约束：不直接操作HTTP请求/响应对象         │
└────────────────┬────────────────────────┘
                 │ 调用
                 ▼
┌─────────────────────────────────────────┐
│   models/ (ORM) + core/ (基础设施)        │
│  职责：数据持久化、缓存、消息队列、        │
│        外部API调用（Dify/短信）           │
│  约束：不包含业务判断逻辑                  │
└─────────────────────────────────────────┘
```

### 5.3 关键技术决策与PRD映射

| 设计决策 | 说明 | 对应PRD |
|----------|------|--------|
| **全异步** | FastAPI + SQLAlchemy 2.0 async + aioredis + aio-pika + elasticsearch-py[async]，全链路`async/await` | PRD 5.1 核心接口≤200ms |
| **API版本化** | `api/v1/` 目录隔离，为后续V2大版本迭代预留空间 | PRD 3.3 版本规划 |
| **Service层独立** | 业务逻辑与接口层分离，便于单元测试 + 未来拆分微服务 | PRD 3.3 Stage4 后续规划 |
| **MQ消费者独立进程** | `tasks/` 目录下消费者由独立的`ConsumerManager`进程拉起，与API服务容器同部署（MVP）或独立部署（扩展） | PRD 4.7/4.8/4.9 AI异步解耦 |
| **Mock支付开关** | `MOCK_PAYMENT` 环境变量控制，开启时支付/核销直通 | PRD 5.4 Mock挡板 |
| **幂等键设计** | `utils/idempotent.py` 统一幂等键生成与校验，所有资金操作强制幂等 | PRD 5.3 资金安全 |
| **ES为检索副本** | MySQL为唯一数据源，ES通过MQ异步同步，数据丢失不影响核心交易 | PRD 4.3 LBS雷达 |

---

> 文档版本：V1.0  
> 编写日期：2026-07-02  
> 对应PRD版本：V3.2（最终落地版）
