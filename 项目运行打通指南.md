# 项目运行打通指南

---

## 1. 文档解读与项目概览

### 1.1 核心功能模块（PRD V3.2）

根据 PRD 和工程设计文档，HalfCart（半仓智拼）是一个超本地化 C2B 预售互助平台，核心模块如下：

| 模块 | PRD章节 | 优先级 | 当前实现状态 |
|------|---------|--------|-------------|
| 用户鉴权（手机号验证码登录/密码登录/微信登录） | 4.1 | P0 | ✅ 已实现 |
| 虚拟账本（双资金字段：wallet_balance + frozen_balance） | 3.2 | P0 | ✅ 已实现 |
| LBS 雷达大厅（附近1.5km拼单瀑布流） | 4.3 | P0 | ✅ 基础实现（ES+Redis GEO） |
| 订单4态状态机（GATHERING→PURCHASING→DELIVERING→FINISHED） | 4.2 | P0 | ✅ 已实现 |
| 资金结算（支付/核销/服务费3%） | 4.5 | P0 | ✅ 已实现（Mock+支付宝沙箱） |
| 阶梯退款（GATHERING全额/PURCHASING扣25%） | 4.6 | P0 | ✅ 已实现 |
| AI 语音/文本发单（ASR + Dify NLP） | 4.7 | P0 | ⚠️ 部分实现（Dify客户端就绪，ASR消费者存根） |
| 现货闪电转让（OCR 小票识别） | 4.8 | P1 | ⚠️ 订单创建已实现，OCR消费者存根 |
| AI 纠纷裁判 | 4.9 | P2 | ⚠️ 数据模型就绪，Dify裁判消费者存根 |
| 提现申请 | 4.10 | P1 | ✅ 已实现（线下打款模式） |
| 履约通信（WebSocket 1v1聊天+阅后即焚） | 3.1 | P1 | ⚠️ WebSocket路由就绪，消费者存根 |
| 运营后台（用户管理/订单管理/提现审核） | 3.1 | P2 | ✅ 基础CRUD已实现 |
| 增量对账（每小时扫描近24h活跃订单） | 4.5 | P0 | ⚠️ 对账服务就绪，消费者存根 |

### 1.2 技术栈摘要

| 层级 | 技术选型 | 版本 |
|------|---------|------|
| **后端框架** | FastAPI (async) | 0.109+ |
| **ASGI服务器** | Uvicorn | 0.27+ |
| **ORM** | SQLAlchemy 2.0 (async) | 2.0.25+ |
| **数据库** | MySQL 8.0 | 8.0.35 |
| **缓存/锁** | Redis 7 | 7.2 |
| **搜索引擎** | Elasticsearch 8 | 8.11.0 |
| **消息队列** | RabbitMQ | 3.12 |
| **JWT** | PyJWT + HS256 | — |
| **加密** | cryptography (AES-256-CBC) | 41.0+ |
| **支付** | 支付宝沙箱 (RSA2-SHA256) | — |
| **AI** | Dify API (NLP/OCR/裁判) | v1 |
| **前端** | 原生 HTML + Vue 3 + Vant 4 | — |
| **反向代理** | Nginx | 1.25 |
| **容器化** | Docker Compose | v2.20+ |

### 1.3 与设计文档的关键差异

> ⚠️ 以下差异**不影响启动**，但需要了解以免误解功能完整性：

1. **目录命名偏差**：设计文档规划 `api/v1/users.py`，实际为 `api/v1/user.py`；`participants.py` 合并入 `orders.py`
2. **中间件偏差**：`cors.py` 和 `trace.py` 内联到 `main.py` 和 `request_log.py` 中
3. **服务层偏差**：`settlement_service.py` 和 `refund_service.py` 合并到 `balance_service.py` 和 `alipay_service.py`
4. **MQ消费者：AI消费者（ASR/OCR/纠纷）均为存根**，不影响启动但不会执行实际AI处理
5. **测试目录**：设计文档规划的 `tests/` 目录存在但无实际测试代码
6. **Alembic迁移**：`migrations/` 目录存在但无迁移脚本，建表由 SQLAlchemy `create_all` 自动完成
7. **`pyproject.toml`** 不存在，依赖管理使用 `requirements.txt` + pip

---

## 2. 环境准备

### 2.1 推荐方式：Docker Compose 一键启动

| 软件 | 最低版本 | 验证命令 |
|------|---------|---------|
| **Docker Desktop** | 24.0+ | `docker --version` |
| **Docker Compose** | v2.20+（包含在Docker Desktop中） | `docker compose version` |

### 2.2 备选方式：本地Python开发

| 软件 | 最低版本 | 验证命令 |
|------|---------|---------|
| **Python** | 3.11.x | `python --version` |
| **Docker Desktop** | 24.0+ | （仅用于启动中间件） |

### 2.3 Docker 资源配置

打开 **Docker Desktop → Settings → Resources**，建议分配：

| 资源 | 最低 | 建议 |
|------|------|------|
| CPU | 2 cores | 4 cores |
| Memory | 4 GB | 8 GB |
| Disk | 10 GB | 20 GB |

---

## 3. 依赖安装

### 3.1 方式一：Docker Compose（推荐）

**无需手动安装任何依赖**，Docker 会自动构建镜像并安装所有依赖。

```bash
# 进入项目根目录
cd C:\Users\Lenovo\Desktop\Half

# 一键启动（首次运行需 3-5 分钟拉取/构建镜像）
docker compose up -d
```

镜像拉取/构建过程：
- MySQL、Redis、ES、RabbitMQ、Nginx → 从 Docker Hub 拉取官方镜像
- Backend → 基于 `python:3.11-slim` 构建，`pip install -r requirements.txt`
- Frontend → 基于 `nginx:1.25-alpine` 构建（当前为占位页面）

### 3.2 方式二：本地 Python 开发

```bash
# 1. 进入后端目录
cd C:\Users\Lenovo\Desktop\Half\backend

# 2. 创建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境
# Windows (Git Bash / CMD):
source .venv/Scripts/activate
# 或 .venv\Scripts\activate

# 4. 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> **注意**：`aiomysql` 需要编译 `libmariadb-dev-compat`，在 Windows 上直接 pip install 可能失败。  
> **Windows 用户建议**：优先使用 Docker Compose 方式，或安装预编译的 wheel。

---

## 4. 配置准备

### 4.1 Docker Compose 模式

项目根目录已有 `.env` 文件，Docker Compose 会自动读取。如从头开始：

```bash
cd C:\Users\Lenovo\Desktop\Half
copy .env.example .env   # 如已有则跳过
```

Docker Compose 模式下 `.env` 关键配置（**已填充，无需修改即可启动**）：

```ini
# MySQL
MYSQL_ROOT_PASSWORD=halfcart_root_2024
MYSQL_DATABASE=halfcart
MYSQL_USER=halfcart
MYSQL_PASSWORD=halfcart123

# Redis
REDIS_PASSWORD=halfcart_redis_2024

# ES (安全已禁用，开发模式)
ES_PASSWORD=elastic123

# RabbitMQ
RABBITMQ_DEFAULT_USER=halfcart
RABBITMQ_DEFAULT_PASS=halfcart123

# 后端
JWT_SECRET_KEY=dev-halfcart-jwt-secret-key-change-in-production
MOCK_PAYMENT=true          # ← MVP关键：开启Mock支付
APP_ENV=development        # ← 开启Swagger文档和测试接口
TEST_MODE=true             # ← 开启测试接口

# Dify AI（如不调用AI功能，可留空不影响启动）
DIFY_API_KEY=app-xxx       # 可选

# 短信（如不发短信，可留空不影响启动）
SMS_ACCESS_KEY=            # 可选
SMS_SECRET_KEY=            # 可选
```

### 4.2 本地开发模式

在 `backend/` 目录创建 `.env`：

```bash
cd C:\Users\Lenovo\Desktop\Half\backend
copy .env.example .env
```

**关键修改**：HOST 全部改为 `localhost`（因为中间件端口已映射到宿主机）：

```ini
DB_HOST=localhost          # Docker中mysql，本地用localhost
REDIS_HOST=localhost       # 同上
REDIS_PASSWORD=halfcart_redis_2024  # 本地Redis需要密码
ES_HOST=http://localhost:9200
ES_PASSWORD=elastic123
MQ_HOST=localhost
MQ_PASSWORD=halfcart123
MOCK_PAYMENT=true
APP_ENV=development
```

### 4.3 配置项说明

| 配置项 | 含义 | 如何获取 | 是否必填 |
|--------|------|---------|---------|
| `JWT_SECRET_KEY` | JWT签名密钥 | 随机生成32+字符 | ✅ 必填 |
| `PHONE_ENCRYPTION_KEY` | AES手机号加密密钥 | 随机生成32字节 | ✅ 必填 |
| `MOCK_PAYMENT` | Mock支付开关 | `true` = 跳过真实支付 | ✅ 必填 |
| `APP_ENV` | 运行环境 | `development` / `production` | ✅ 必填 |
| `TEST_MODE` | 测试接口开关 | `true` = 开启 `/api/v1/test/*` | ✅ 必填 |
| `DIFY_API_KEY` | Dify AI平台密钥 | https://dify.ai 注册获取 | ❌ 可选 |
| `SMS_ACCESS_KEY` | 阿里云短信 AccessKey | 阿里云RAM控制台 | ❌ 可选 |
| `SMS_SECRET_KEY` | 阿里云短信 SecretKey | 同上 | ❌ 可选 |
| `ALIPAY_APP_ID` | 支付宝应用ID | 支付宝开放平台 | ❌ 可选 |
| `WECHAT_APP_ID` | 微信 AppID | 微信公众平台 | ❌ 可选 |
| `AMAP_WEB_KEY` | 高德地图 Web Key | 高德开放平台 | ❌ 可选 |

---

## 5. 数据库初始化

### 5.1 Docker Compose 模式（自动）

数据库初始化**全自动**，无需手动操作：

1. MySQL 容器首次启动时，执行 `deploy/mysql/init/01-init-database.sql` 创建数据库
2. Backend 容器启动时，`main.py` 的 `lifespan` 函数调用 `init_db()`
3. `init_db()` 执行 `Base.metadata.create_all` 自动创建所有表（幂等）

### 5.2 本地开发模式

```bash
# 1. 确保 Docker 中间件已启动
cd C:\Users\Lenovo\Desktop\Half
docker compose up -d mysql redis elasticsearch rabbitmq

# 2. 手动创建数据库（首次）
docker exec -it halfcart-mysql mysql -u root -phalfcart_root_2024 -e "CREATE DATABASE IF NOT EXISTS halfcart DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 3. 启动后端时自动建表（SQLAlchemy create_all）
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# 首次启动时会输出 "✅ [1/4] MySQL 连接池初始化完成"（含自动建表）
```

### 5.3 验证数据库

```bash
# 进入 MySQL 容器
docker exec -it halfcart-mysql mysql -u halfcart -phalfcart123 halfcart

# 查看表列表
SHOW TABLES;
```

预期输出：
```
+-----------------------------+
| Tables_in_halfcart          |
+-----------------------------+
| t_alipay_records            |
| t_chat_messages             |
| t_disputes                  |
| t_order_participants        |
| t_orders                    |
| t_payment_logs              |
| t_reconciliation_anomalies  |
| t_reconciliation_records    |
| t_sms_logs                  |
| t_user_addresses            |
| t_users                     |
| t_verification_codes        |
| t_withdraw_applications     |
+-----------------------------+
```

---

## 6. 启动步骤

### 6.1 方式一：Docker Compose 全量启动（推荐 ⭐）

```bash
# Step 1: 进入项目根目录
cd C:\Users\Lenovo\Desktop\Half

# Step 2: 启动所有服务（首次需拉取镜像 3-5 分钟）
docker compose up -d

# Step 3: 等待所有服务 healthy（约 60-90 秒）
docker compose ps --format "table {{.Name}}\t{{.Status}}"
```

**预期输出**（所有服务 Status = Up (healthy)）：

```
NAME                    STATUS
halfcart-mysql          Up (healthy)
halfcart-redis          Up (healthy)
halfcart-elasticsearch  Up (healthy)
halfcart-rabbitmq       Up (healthy)
halfcart-backend        Up (healthy)
halfcart-consumer       Up (healthy)
halfcart-frontend       Up (healthy)
halfcart-nginx          Up (healthy)
```

**后端启动成功日志特征**（`docker compose logs backend`）：

```
============================================================
  HalfCart v1.0.0 启动中...
  环境: development | Mock支付: True
============================================================
✅ [1/4] MySQL 连接池初始化完成
✅ [2/4] Redis 连接池初始化完成
✅ [3/4] ElasticSearch 客户端初始化完成
✅ [4/4] RabbitMQ 连接初始化完成
🚀 HalfCart v1.0.0 启动完成，等待请求...
```

> ⚠️ **ES 启动可能失败**（Windows WSL `vm.max_map_count` 不足），不影响核心业务——后端会输出警告并继续运行：`⚠️ ElasticSearch 初始化失败 (LBS搜索降级)`

### 6.2 方式二：Docker 中间件 + 本地 Python

```bash
# Terminal 1: 仅启动中间件
cd C:\Users\Lenovo\Desktop\Half
docker compose up -d mysql redis elasticsearch rabbitmq

# Terminal 2: 本地启动后端（支持热重载）
cd C:\Users\Lenovo\Desktop\Half\backend
# 激活虚拟环境后：
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 6.3 各服务访问地址

| 服务 | 地址 | 用途 |
|------|------|------|
| **Backend API文档** | http://localhost:8000/docs | Swagger UI 交互式接口文档 |
| **Backend 健康检查** | http://localhost:8000/health | 返回应用状态 |
| **Backend 就绪探针** | http://localhost:8000/health/readiness | 返回中间件连接状态 |
| **RabbitMQ 管理台** | http://localhost:15672 | 用户名 `halfcart` / 密码 `halfcart123` |
| **Frontend H5** | http://localhost:3000 | 前端占位页面 |
| **Nginx 统一入口** | http://localhost | 反向代理（API + 前端） |
| **ES 集群健康** | http://localhost:9200/_cluster/health | ES 状态检查 |

---

## 7. 冒烟测试

以下测试按难度递增，验证核心链路是否连通。

### 7.1 测试 1：健康检查（验证服务存活）

```bash
curl http://localhost:8000/health
```

**预期返回**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "app": "HalfCart",
    "version": "1.0.0",
    "environment": "development"
  }
}
```

### 7.2 测试 2：就绪探针（验证中间件连接）

```bash
curl http://localhost:8000/health/readiness
```

**预期返回**：
```json
{
  "code": 0,
  "message": "ready",
  "data": {
    "ready": true,
    "components": {
      "mysql": "healthy",
      "redis": "healthy"
    }
  }
}
```

### 7.3 测试 3：发送登录验证码（验证短信/Redis链路）

```bash
curl -X POST http://localhost:8000/api/v1/auth/send-login-code \
  -H "Content-Type: application/json" \
  -d '{"phone": "13800138000"}'
```

**预期返回**（Mock支付模式下，验证码存储在Redis中）：
```json
{
  "code": 0,
  "message": "验证码已发送",
  "data": {
    "sent": true,
    "expires_in": 300
  }
}
```

> ⚠️ 如果 `SMS_ACCESS_KEY` 未配置，短信发送会失败。但 Mock 模式下不影响后续流程（可通过测试接口或从Redis读取验证码）。

### 7.4 测试 4：创建测试用户+获取Token（验证完整鉴权链路）

```bash
# Step 1: 创建测试用户
curl -X POST http://localhost:8000/api/v1/test/balance/create-user \
  -H "Content-Type: application/json" \
  -d '{}'

# 返回示例: {"code":0,"data":{"user_id":1,"nickname":"TestUser_xxx"}}

# Step 2: 用该 user_id 获取 Token（TEST_MODE专用接口）
curl -X POST http://localhost:8000/api/v1/test/token \
  -H "Content-Type: application/json" \
  -d '{"user_id": 1}'

# 返回示例: {"code":0,"data":{"access_token":"eyJ...","token_type":"bearer"}}
```

### 7.5 测试 5：创建订单+查询（验证订单状态机链路）

```bash
# 获取 Token 后（替换 YOUR_TOKEN）
TOKEN="eyJ..."

# 创建普通拼单
curl -X POST http://localhost:8000/api/v1/orders/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "goods_name": "瑞士卷",
    "goods_desc": "山姆会员店瑞士卷",
    "total_people": 3,
    "price_per_person": "29.90",
    "meet_location": "小区东门",
    "location_lat": "30.2741",
    "location_lng": "120.1551"
  }'

# 预期返回: 订单详情（status=0 GATHERING）
```

### 7.6 测试 6：Swagger 文档可用性

浏览器打开 **http://localhost:8000/docs**，应看到：
- 右上角 **「Authorize」** 按钮（🔒 锁图标）
- 接口分组：Auth、Orders、LBS Radar、User Center、Payment-Alipay、Admin 等
- 点击 "Try it out" 可直接在页面上测试接口

---

## 8. 常见启动错误及解决方案

### ❌ 错误 1: `vm.max_map_count is too low`（ES 启动失败）

**错误信息**：
```
elasticsearch | ERROR: [1] bootstrap checks failed.
elasticsearch | max virtual memory areas vm.max_map_count [65530] is too low, increase to at least [262144]
```

**解决方案**：
```powershell
# Windows (PowerShell, 管理员模式):
wsl -d docker-desktop
sysctl -w vm.max_map_count=262144
exit
docker compose restart elasticsearch
```

**备选**：ES 启动失败**不阻塞后端启动**，后端会降级运行（LBS搜索不可用，其他功能正常）。

---

### ❌ 错误 2: 端口被占用

**错误信息**：
```
Error response from daemon: Ports are not available: listen tcp 0.0.0.0:3306: bind: address already in use
```

**解决方案**：
```bash
# Windows: 查找占用端口的进程
netstat -ano | findstr :3306
taskkill /PID <PID> /F

# 或修改 docker-compose.yml 端口映射（如 3307:3306）
```

---

### ❌ 错误 3: Backend 启动后立即退出（`exited`）

**排查**：
```bash
docker compose logs backend --tail=30
```

**常见原因**：
- MySQL 未就绪 → 等待 `halfcart-mysql` 状态为 `healthy` 后重启
- `.env` 密码与 Docker Compose 中设置不一致
- Python 依赖安装失败（网络问题）

```bash
# 重建后端镜像
docker compose build backend --no-cache
docker compose up -d backend
```

---

### ❌ 错误 4: MySQL 连接被拒绝

**错误信息**：
```
sqlalchemy.exc.OperationalError: (2003, "Can't connect to MySQL server on 'mysql'")
```

**解决**：
```bash
# 检查 MySQL 是否 healthy
docker compose ps mysql

# 等待 healthy 后重启后端
docker compose restart backend
```

---

### ❌ 错误 5: RabbitMQ 长时间 `starting`

**原因**：首次启动需初始化数据库，约 30-60 秒。

**检查**：
```bash
docker compose logs rabbitmq --tail=10
# 看到 "Server startup complete; ..." 即完成
```

---

### ❌ 错误 6: Docker 镜像拉取慢

**配置国内镜像加速器** — Docker Desktop → Settings → Docker Engine：
```json
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.xuanyuan.me"
  ]
}
```

---

### ❌ 错误 7: `pip install` 失败（本地模式）

**Windows 上 `aiomysql` 编译失败**：

```bash
# 方案1：使用预编译wheel
pip install aiomysql --only-binary :all:

# 方案2：放弃本地模式，改用 Docker Compose
docker compose up -d
```

---

## 9. 最终验证与后续建议

### 9.1 项目启动成功确认清单

| # | 检查项 | 验证方法 | 状态 |
|---|--------|---------|------|
| 1 | Docker 容器全部 healthy | `docker compose ps` | ☐ |
| 2 | 健康检查返回 OK | `curl localhost:8000/health` | ☐ |
| 3 | MySQL 可连接 | `docker exec -it halfcart-mysql mysql -u halfcart -phalfcart123 -e "SELECT 1"` | ☐ |
| 4 | Redis 可连接 | `docker exec -it halfcart-redis redis-cli -a halfcart_redis_2024 PING` | ☐ |
| 5 | 数据库表已创建 | 见 5.3 节验证命令 | ☐ |
| 6 | Swagger 文档可访问 | 浏览器 http://localhost:8000/docs | ☐ |
| 7 | 验证码接口可调用 | 见 7.3 节测试 | ☐ |
| 8 | Token 可获取 | 见 7.4 节测试 | ☐ |

### 9.2 已实现 vs 未实现功能

**✅ 可立即验证的核心功能**：
- 用户鉴权（验证码登录/密码登录/微信登录/Token刷新）
- 订单CRUD（创建普通订单/现货订单、列表查询、详情、状态流转）
- 资金操作（冻结增加/扣减、钱包出入账、核销结算、违约金划转）
- 支付宝沙箱支付（创建支付/异步回调/退款）
- LBS 雷达（附近订单查询）
- 用户管理（个人信息、地址管理、信用分）
- 运营后台（用户管理/订单管理/提现审核/对账）
- 测试接口（快速创建测试用户、获取Token、资金操作）

**⚠️ 存根/占位功能（不阻塞启动，但不可用）**：
- AI 语音发单（Dify 客户端就绪，MQ 消费者为存根）
- AI OCR 现货转让（同上）
- AI 纠纷裁判（同上）
- WebSocket 实时聊天（路由就绪，消息推送消费者为存根）
- 阅后即焚清理（定时任务消费者为存根）

**❌ 未开始的功能**：
- 前端 Vue3 SPA（当前为占位 HTML 页面）
- 自动化测试（`tests/` 目录为空）
- Alembic 数据库迁移脚本

### 9.3 下一步建议优先级

根据之前的审计报告和当前项目状态，建议按以下顺序推进：

| 优先级 | 任务 | 说明 |
|--------|------|------|
| **🔴 P0-立即** | 修复安全漏洞 | `.env` 密钥保护、测试接口鉴权、手机号加密 |
| **🔴 P0-立即** | 修复 `TEST_MODE` 默认值 | 改为 `False` |
| **🟠 P1-本周** | 完善 MQ 消费者 | AI 发单/OCR/纠纷消费者实现 |
| **🟠 P1-本周** | 前端 Vue3 重构 | 替换占位 HTML 为完整 SPA |
| **🟡 P2-两周** | 编写自动化测试 | 至少覆盖核心交易链路和并发测试 |
| **🟡 P2-两周** | 补充 Alembic 迁移 | 替代 `create_all` 自动建表 |
| **🟢 P3-一个月** | 对接真实支付渠道 | 关闭 `MOCK_PAYMENT`，对接支付宝正式环境 |
| **🟢 P3-一个月** | CI/CD 流水线 | 自动构建、测试、安全扫描 |

---

> 📞 如遇文档未覆盖的启动问题，请执行以下命令收集诊断信息：
> ```bash
> docker compose ps -a > diagnose.txt
> docker compose logs --tail=100 >> diagnose.txt
> ```
> 将 `diagnose.txt` 发送给开发团队进行分析。
