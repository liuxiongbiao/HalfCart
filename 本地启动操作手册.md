# 半仓智拼 (HalfCart) 本地启动操作手册

> 适用版本：V1.0  
> 最后更新：2026-07-02  
> 前置阅读：[工程底座设计文档](工程底座设计文档.md)

---

## 1. 项目目录结构

```
Half/                                    # 项目根目录
├── docker-compose.yml                   # ⭐ Docker 全量编排文件（一键启动）
├── .env                                 # 环境变量（Docker Compose 自动读取）
├── .env.example                         # 环境变量模板
├── 工程底座设计文档.md                    # 架构设计文档
├── 本地启动操作手册.md                    # 📖 本文档
├── deploy/                              # 中间件配置文件
│   ├── mysql/
│   │   ├── init/
│   │   │   └── 01-init-database.sql     # 数据库初始化 SQL
│   │   └── conf.d/
│   │       └── my.cnf                   # MySQL 自定义配置
│   ├── redis/
│   │   └── redis.conf                   # Redis 自定义配置
│   ├── es/
│   │   └── elasticsearch.yml            # ES 单节点配置
│   └── nginx/
│       └── nginx.conf                   # Nginx 反向代理配置
├── data/                                # ⚠️ 容器数据持久化目录（gitignore）
│   ├── mysql/                           # MySQL 数据文件
│   ├── redis/                           # Redis RDB/AOF
│   ├── es/                              # ES 索引数据
│   └── rabbitmq/                        # RabbitMQ 消息持久化
├── backend/                             # 后端 Python 项目
│   ├── Dockerfile                       # 后端容器镜像
│   ├── pyproject.toml                   # Poetry 依赖管理
│   ├── .env.example                     # 后端环境变量模板
│   ├── app/                             # 应用源码
│   │   ├── main.py                      # FastAPI 入口
│   │   ├── config.py                    # 配置中心 (pydantic-settings)
│   │   ├── api/                         # 接口层 (Router)
│   │   │   ├── deps.py                  # 依赖注入
│   │   │   ├── v1/                      # API v1 路由
│   │   │   └── websocket/              # WebSocket 路由
│   │   ├── core/                        # 核心基础设施层
│   │   │   ├── database.py             # SQLAlchemy 异步引擎
│   │   │   ├── redis_client.py         # Redis 客户端 + 分布式锁
│   │   │   ├── es_client.py            # ES 8 异步客户端
│   │   │   ├── mq_client.py            # RabbitMQ 异步生产者
│   │   │   └── security.py            # JWT + AES 加密
│   │   ├── models/                      # ORM 数据模型
│   │   ├── schemas/                     # Pydantic Schema
│   │   ├── services/                    # 业务逻辑层
│   │   ├── tasks/                       # MQ 消费者 + 定时任务
│   │   ├── middleware/                  # 中间件
│   │   │   ├── exception_handler.py    # 全局异常处理
│   │   │   └── request_log.py          # 请求日志 + trace_id
│   │   ├── utils/                       # 工具函数
│   │   │   ├── constants.py            # 全局枚举常量
│   │   │   ├── idempotent.py           # 幂等键生成
│   │   │   └── privacy.py             # 隐私脱敏
│   │   └── migrations/                  # Alembic 数据库迁移
│   └── tests/                           # 测试目录
└── frontend/                            # 前端 Vue3 项目（后续创建）
    └── Dockerfile
```

---

## 2. 环境前置要求

### 2.1 必须安装的软件

| 软件 | 最低版本 | 说明 | 验证命令 |
|------|---------|------|---------|
| **Docker Desktop** | 24.0+ | 容器运行环境 (Windows/Mac) | `docker --version` |
| **Docker Compose** | v2.20+ | 多容器编排 (已包含在 Docker Desktop 中) | `docker compose version` |
| **Git** | 2.40+ | 版本管理 | `git --version` |

> **注意**：Docker Desktop 安装后会自动包含 Docker Compose V2。  
> 旧版 `docker-compose` (带连字符) 已弃用，请使用 `docker compose` (空格)。

### 2.2 可选安装（用于本地调试）

| 软件 | 最低版本 | 说明 | 验证命令 |
|------|---------|------|---------|
| **Python** | 3.11.x | 本地运行后端（方式二需要） | `python --version` |
| **Poetry** | 1.7+ | Python 依赖管理 | `poetry --version` |

### 2.3 Docker 资源配置建议

打开 Docker Desktop → Settings → Resources，建议分配：

| 资源 | 建议值 | 用途 |
|------|--------|------|
| CPU | 4 cores | MySQL + ES + RabbitMQ 并行运行 |
| Memory | 8 GB | ES 堆内存（512MB）+ MySQL 缓冲池（256MB）+ 各服务基础开销 |
| Swap | 2 GB | 内存溢出兜底 |
| Disk | 20 GB | 数据持久化 + 镜像存储 |

---

## 3. 方式一：Docker Compose 一键全量启动（推荐 ⭐）

### 3.1 只需 3 步

```bash
# 步骤 1：进入项目根目录
cd Half

# 步骤 2：启动所有服务（首次约 3-5 分钟拉取镜像）
docker compose up -d

# 步骤 3：等待启动完成，查看状态
docker compose ps
```

### 3.2 各服务访问地址

启动成功后，以下地址可直接访问：

| 服务 | 地址 | 用途 |
|------|------|------|
| **后端 API 文档** | http://localhost:8000/docs | Swagger UI 交互式文档 |
| **后端健康检查** | http://localhost:8000/health | 返回 `{"status": "ok"}` |
| **后端就绪探针** | http://localhost:8000/health/readiness | 返回各组件连接状态 |
| **RabbitMQ 管理台** | http://localhost:15672 | 用户名 `halfcart` 密码 `halfcart123` |
| **统一入口（Nginx）** | http://localhost | 前端 H5 页面（需前端构建完成） |

### 3.3 常用运维命令

```bash
# ── 服务管理 ──────────────────────
docker compose up -d                          # 启动所有服务（后台运行）
docker compose down                            # 停止并删除所有容器
docker compose down -v                         # ⚠️ 停止并删除所有数据卷
docker compose restart <service_name>          # 重启指定服务（如 backend）
docker compose up -d --build backend           # 重新构建并启动后端

# ── 日志查看 ──────────────────────
docker compose logs -f                        # 实时查看所有服务日志
docker compose logs -f backend                # 仅查看后端日志
docker compose logs --tail=100 backend        # 查看后端最近100行日志
docker compose logs --since=10m backend       # 查看后端最近10分钟日志

# ── 状态检查 ──────────────────────
docker compose ps                             # 查看所有容器状态
docker compose ps -a                          # 查看所有容器（含已停止）
docker stats                                  # 实时查看容器资源占用

# ── 进入容器调试 ──────────────────
docker exec -it halfcart-backend bash         # 进入后端容器
docker exec -it halfcart-mysql mysql -u halfcart -p  # 进入MySQL
docker exec -it halfcart-redis redis-cli -a halfcart_redis_2024  # 进入Redis
```

### 3.4 启动流程说明（内部）

当你执行 `docker compose up -d` 时，系统按以下顺序启动：

```
第1阶段（并发启动）
  ├── mysql     → 等待 healthy
  ├── redis     → 等待 healthy
  ├── elasticsearch → 等待 healthy
  └── rabbitmq  → 等待 healthy
       │
第2阶段（依赖第1阶段全部 healthy）
  └── backend   → 连接 MySQL/Redis/ES/MQ → 创建 ES 索引 → 声明 MQ 队列
       │
第3阶段
  ├── frontend  → 启动 Nginx 静态服务
  └── nginx     → 启动反向代理
```

后端启动时的日志（`docker compose logs backend`）：

```
============================================================
  HalfCart v1.0.0 启动中...
  环境: development | Mock支付: True
============================================================
✅ [1/4] MySQL 连接池初始化完成
✅ [2/4] Redis 连接池初始化完成
✅ [3/4] ElasticSearch 客户端初始化完成
✅ [4/4] RabbitMQ 连接初始化完成
🚀 HalfCart v1.0.0 启动完成，等待请求...
```

### 3.5 常见启动问题与解决

#### ❌ 问题 1: `vm.max_map_count is too low`（ElasticSearch 启动失败）

**错误信息**：
```
ERROR: [1] bootstrap checks failed. max virtual memory areas vm.max_map_count [65530] is too low
```

**原因**：ES 需要系统的虚拟内存映射数至少 262144。

**解决方法（Windows Docker Desktop）**：
```powershell
# 1. 进入 WSL 终端
wsl -d docker-desktop

# 2. 设置 vm.max_map_count
sysctl -w vm.max_map_count=262144

# 3. 验证设置
sysctl vm.max_map_count
# 输出: vm.max_map_count = 262144

# 4. 退出 WSL
exit

# 5. 重启 ES
docker compose restart elasticsearch
```

**解决方法（Mac）**：
```bash
# Docker Desktop → Settings → 自动管理，无需额外配置
```

**永久生效（Linux）**：
```bash
sudo sh -c 'echo "vm.max_map_count=262144" >> /etc/sysctl.conf'
sudo sysctl -p
```

---

#### ❌ 问题 2: 端口被占用

**错误信息**：
```
Error response from daemon: Ports are not available: listen tcp 0.0.0.0:3306: bind: address already in use
```

**解决方法**：

**Windows**：
```powershell
# 查找占用端口的进程
netstat -ano | findstr :3306

# 找到 PID 后（如 1234），结束进程
taskkill /PID 1234 /F
```

**Mac / Linux**：
```bash
# 查找占用端口的进程
lsof -i :3306

# 结束进程（替换 PID）
kill -9 <PID>
```

**备选方案**：修改 `docker-compose.yml` 中的端口映射，如 `3307:3306`。

---

#### ❌ 问题 3: RabbitMQ 启动特别慢

**现象**：`halfcart-rabbitmq` 长时间处于 `starting` 状态。

**原因**：RabbitMQ 首次启动需要初始化数据库和插件，约需 30-60 秒。

**检查**：
```bash
# 等待 60 秒后检查状态
docker compose logs rabbitmq --tail=20
# 看到 "Server startup complete" 表示启动完成
```

**如果持续启动失败**：
```bash
# 清理数据重新启动
docker compose down
rm -rf data/rabbitmq/*
docker compose up -d rabbitmq
```

---

#### ❌ 问题 4: Backend 启动后立即退出

**排查步骤**：
```bash
# 1. 查看后端日志
docker compose logs backend

# 2. 常见原因：
#    - MySQL 未就绪 → 等待 mysql healthcheck 通过后重启
#    - 数据库连接失败 → 检查 .env 中的数据库密码
docker compose restart backend
```

---

#### ❌ 问题 5: ES 磁盘空间不足导致只读

**现象**：ES 日志中出现 `disk usage exceeded flood stage watermark`。

**解决**：
```bash
# 清理磁盘或调整水位线
docker exec -it halfcart-elasticsearch curl -XPUT "http://localhost:9200/_cluster/settings" \
  -H 'Content-Type: application/json' \
  -d '{"transient":{"cluster.routing.allocation.disk.watermark.flood_stage":"95%"}}'
```

---

#### ❌ 问题 6: Docker 镜像拉取太慢

**解决方法**：配置国内镜像加速器。

**Docker Desktop → Settings → Docker Engine**，添加：
```json
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.xuanyuan.me"
  ]
}
```

---

## 4. 方式二：本地 Python 调试启动

适用场景：需要断点调试后端代码、频繁修改代码、不想反复构建 Docker 镜像。

### 4.1 安装 Python 依赖

```bash
# 步骤 1: 进入后端目录
cd Half/backend

# 步骤 2: 安装 Poetry（如未安装）
# Mac / Linux:
curl -sSL https://install.python-poetry.org | python3 -
# Windows (PowerShell):
(Invoke-WebRequest -Uri https://install.python-poetry.org -UseBasicParsing).Content | python -

# 步骤 3: 安装项目依赖
poetry install

# 步骤 4: 激活虚拟环境
poetry shell
# 或不用 poetry shell，直接在命令前加 poetry run
```

### 4.2 单独启动中间件（不含后端）

只启动 MySQL + Redis + ES + RabbitMQ，后端在本地 IDE 中运行：

```bash
# 在项目根目录执行
cd Half

# 仅启动中间件（排除 backend / frontend / nginx）
docker compose up -d mysql redis elasticsearch rabbitmq

# 验证中间件状态
docker compose ps
```

此时可访问：
- MySQL: `localhost:3306`（用户 `halfcart`，密码 `halfcart123`）
- Redis: `localhost:6379`（密码 `halfcart_redis_2024`）
- ES: `http://localhost:9200`
- RabbitMQ: `http://localhost:15672` / `localhost:5672`

### 4.3 配置本地环境变量

```bash
# 在 backend/ 目录创建 .env 文件
cd Half/backend
cp .env.example .env
```

编辑 `.env`，确保 `HOST` 指向 `localhost`（因为中间件端口已映射到本机）：

```ini
# ── 关键修改：HOST 全部改为 localhost ──
DB_HOST=localhost
REDIS_HOST=localhost
ES_HOST=http://localhost:9200
MQ_HOST=localhost
```

### 4.4 初始化数据库

```bash
# 在 backend/ 目录执行
cd Half/backend

# 如果安装了 Alembic（后续会补充迁移脚本），执行：
# poetry run alembic upgrade head

# 当前阶段：手动连接 MySQL 创建数据库
docker exec -it halfcart-mysql mysql -u root -phalfcart_root_2024 -e "CREATE DATABASE IF NOT EXISTS halfcart DEFAULT CHARACTER SET utf8mb4;"
```

### 4.5 启动后端服务

```bash
# 方式 A: 使用 uvicorn 命令行（开发模式，支持热重载）
cd Half/backend
poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 方式 B: 使用 VSCode 调试模式
# 在 .vscode/launch.json 中配置：
# {
#     "name": "HalfCart Backend",
#     "type": "debugpy",
#     "request": "launch",
#     "module": "uvicorn",
#     "args": ["app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"],
#     "jinja": true,
#     "cwd": "${workspaceFolder}/backend"
# }
```

启动成功后会看到：
```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     ============================================================
INFO:       HalfCart v1.0.0 启动中...
INFO:     ============================================================
INFO:     ✅ [1/4] MySQL 连接池初始化完成
INFO:     ✅ [2/4] Redis 连接池初始化完成
INFO:     ✅ [3/4] ElasticSearch 客户端初始化完成
INFO:     ✅ [4/4] RabbitMQ 连接初始化完成
INFO:     🚀 HalfCart v1.0.0 启动完成，等待请求...
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 4.6 停止服务

```bash
# 停止本地后端：按 Ctrl + C

# 停止 Docker 中间件：
cd Half
docker compose down
```

---

## 5. 启动成功的判断标准

### 5.1 最低标准（后端服务可用）

在浏览器中访问以下地址，确认返回结果：

#### ✅ 检查点 1：健康检查

**地址**：[http://localhost:8000/health](http://localhost:8000/health)

**期望结果**：
```json
{
    "status": "ok",
    "app": "HalfCart",
    "version": "1.0.0",
    "environment": "development"
}
```

#### ✅ 检查点 2：就绪探针（所有中间件连接正常）

**地址**：[http://localhost:8000/health/readiness](http://localhost:8000/health/readiness)

**期望结果**：
```json
{
    "status": "ready",
    "components": {
        "mysql": "healthy",
        "redis": "healthy"
    }
}
```

#### ✅ 检查点 3：API 文档页

**地址**：[http://localhost:8000/docs](http://localhost:8000/docs)

**期望结果**：Swagger UI 页面正常渲染，显示 API 接口列表。

### 5.2 完整标准（所有中间件可达）

| 检查项 | 验证方法 | 期望结果 |
|--------|---------|---------|
| MySQL 连接 | `docker exec -it halfcart-mysql mysql -u halfcart -phalfcart123 -e "SELECT 1"` | 输出 `1` |
| Redis 连接 | `docker exec -it halfcart-redis redis-cli -a halfcart_redis_2024 PING` | 输出 `PONG` |
| ES 健康 | 浏览器访问 `http://localhost:9200/_cluster/health` | `"status":"green"` 或 `"yellow"` |
| RabbitMQ 管理台 | 浏览器访问 `http://localhost:15672` | 登录页（用户 `halfcart` 密码 `halfcart123`） |
| 后端 /health | 浏览器访问 `http://localhost:8000/health` | `{"status":"ok",...}` |
| API 文档 | 浏览器访问 `http://localhost:8000/docs` | Swagger UI 页面 |

### 5.3 Docker 容器状态标准

```bash
$ docker compose ps
NAME                    STATUS                    PORTS
halfcart-mysql          Up (healthy)              0.0.0.0:3306->3306/tcp
halfcart-redis          Up (healthy)              0.0.0.0:6379->6379/tcp
halfcart-elasticsearch  Up (healthy)              0.0.0.0:9200->9200/tcp
halfcart-rabbitmq       Up (healthy)              0.0.0.0:5672->5672/tcp, 0.0.0.0:15672->15672/tcp
halfcart-backend        Up (healthy)              0.0.0.0:8000->8000/tcp
```

**关键**：所有容器的 STATUS 都是 `Up (healthy)`，不能有 `unhealthy` 或 `starting` 或 `restarting`。

### 5.4 如果检查点未通过

```bash
# 1. 查看具体哪个容器异常
docker compose ps

# 2. 查看异常容器的日志
docker compose logs <container_name> --tail=50
# 如: docker compose logs backend --tail=50
# 如: docker compose logs elasticsearch --tail=50

# 3. 重启异常容器
docker compose restart <container_name>

# 4. 还是不行的终极方案：全部重来
docker compose down -v          # ⚠️ 会删除数据卷
docker compose up -d --build    # 重新构建并启动
```

---

## 6. 快速参考卡片

```bash
# ═══════════════════════════════════════════════
# 一键启动
# ═══════════════════════════════════════════════
cd Half && docker compose up -d

# ═══════════════════════════════════════════════
# 常用地址
# ═══════════════════════════════════════════════
# 后端 API 文档:    http://localhost:8000/docs
# 后端健康检查:     http://localhost:8000/health
# RabbitMQ 管理台:  http://localhost:15672 (halfcart / halfcart123)
# 统一入口:         http://localhost

# ═══════════════════════════════════════════════
# 常用命令
# ═══════════════════════════════════════════════
docker compose ps                    # 查看状态
docker compose logs -f backend       # 后端日志
docker compose restart backend       # 重启后端
docker compose down                  # 停止全部

# ═══════════════════════════════════════════════
# 本地调试 (中间件用 Docker，后端用 IDE)
# ═══════════════════════════════════════════════
docker compose up -d mysql redis elasticsearch rabbitmq  # 仅中间件
cd backend && poetry run uvicorn app.main:app --reload   # 本地后端
```

---

> 📞 遇到问题？请先检查第 3.5 节的常见问题解决方案。如仍无法解决，查看 Docker 日志：  
> `docker compose logs --tail=100 > debug.log` 并将 `debug.log` 发给开发团队。
